<form class="d-inline-block" method="post" action="{{ path('type_diplome_delete', {'id': type_diplome.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_diplome.id) }}">
    <button class="btn btn-danger mb-0"> 
        <i data-feather="trash" class="pr-1"></i>
        <span class="pl-1 align-middle">Delete</span>
    </button>
</form>
